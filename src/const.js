const FilterType = {
  EVERYTHING: 'everything',
  FUTURE: 'future'
};

const Mode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING'
};

const SortType = {
  DATE_UP: 'date-up',
  PRICE_DOWN: 'price-down'
};

export {SortType ,FilterType, Mode};
